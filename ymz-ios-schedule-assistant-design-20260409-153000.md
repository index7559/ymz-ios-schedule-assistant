# 设计文档：AI日程管理应用

Generated by /office-hours on 2026-04-09
Branch: unknown
Repo: local-only
Status: APPROVED (post-eng-review修订)
Mode: Startup

## 产品概述

一款基于大模型与Agent技术的智能日程管理应用，通过自然语言交互（LUI）为核心，结合图形界面，解决用户在日程安排中的痛点。

核心交互模式：
- 语音或文本输入 → AI解析意图 → 列出选项等待用户打钩确认 → 生成日程
- 所有普通日历app的基础功能（创建、查看、修改、删除、月视图）
- 月视图作为iOS右侧负一屏（向右滑出），左滑为日程列表

## 需求澄清（与用户确认的决策）

### 1. 意图处理原则
- 当用户意图不完整时，AI直接追问，不猜
- 例如："开完会我要先整理记录再打" → 追问"整理记录需要多长时间？"

### 2. 提醒机制
- 手机系统通知，走标准iOS日历通知体系
- AI对话内的提醒是辅助说明，不是主力通知

### 3. 打钩框语义
- **AI确认框**：对话解析完成后，列出选项，用户打钩生成日程
- **完成框**：日程列表中，事项完成时打钩，打钩后变灰+文字中间横线
- 两个机制独立，不重复

### 4. 语音输入技术方案
- ASR层：iOS原生 `SFSpeechRecognizer`（免费，本地端，转文字）
- LLM层：火山方舟 `ark-code-latest`（解析意图、生成日程选项）
- ASR和LLM分层，不混用

### 5. AI Agent运行位置
- **LLM调用**：iPhone端直调火山方舟ark-code-latest，API Key存Keychain
- **固定规则引擎**：iPhone本地（冲突检测/UNNotification注册。注：时间解析和提醒计算已由LLM完成，规则引擎不做重复工作）
- **MacBook**：纯数据库+同步节点，不跑LLM，不跑规则引擎
- **离线策略**：LLM无法离线推理；待确认选项存iPhone本地SQLite；已确认日程联网后同步MacBook

### 6. 月视图设计
- 完整月历（每个日期显示该天所有日程）
- 点击任意日程可直接编辑/删除（弹出编辑界面，类普通日历app交互）
- 不走AI对话，直接操作

### 7. 日程列表设计
- 左滑为日程列表，向上滚动从当前时间往前推
- 可切换按日期筛选，查看特定日期内所有日程

## 系统架构

### 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         iPhone 客户端                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐              │
│  │  对话界面  │  │ 日程列表  │  │    月视图         │              │
│  │ （主界面） │  │ （左滑）  │  │  （右滑负一屏）   │              │
│  └──────────┘  └──────────┘  └──────────────────┘              │
│                                                                  │
│  ┌──────────────────────────────────────────────────────┐       │
│  │  本地SQLite：                                        │       │
│  │  · pending_schedules（已确认日程，is_synced标记同步状态）│       │
│  │  · pending_input（待LLM处理的离线语音输入）           │       │
│  │                                                       │       │
│  │  LLM调用层：                                          │       │
│  │  · ark-code-latest HTTP直调（API Key存Keychain）      │       │
│  │  · 固定规则引擎（冲突检测/UNNotification注册）     │       │
│  │                                                       │       │
│  │  SFSpeechRecognizer（本地，免费，60秒截断）           │       │
│  │  UNUserNotificationCenter（系统本地推送）              │       │
│  └──────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
                          ↕ HTTP/HTTPS
┌─────────────────────────────────────────────────────────────────┐
│                    MacBook 服务器（纯数据库+同步节点）              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              SQLite 数据库（MacBook本地）                  │    │
│  │  · schedules 表（唯一数据源）                            │    │
│  │  · sessions 表（会话管理）                              │    │
│  │  · 无规则引擎，无LLM调用，无业务逻辑                    │    │
│  └────────────────────────────────────────────────────────┘    │
│                                                                  │
│  提供接口：                                                    │
│  · GET/POST/PUT/DELETE /api/schedules （日程CRUD）            │
│  · POST /api/sync （批量同步来自iPhone的日程）                 │
│  · GET/POST /api/sessions （会话管理）                         │
└─────────────────────────────────────────────────────────────────┘
```

### 网络访问策略

| 场景 | 访问方式 | 说明 |
|------|---------|------|
| 同局域网 | 直连 MacBook IP（如 192.168.1.x:3000） | 零延迟，最低成本 |
| 外出/远程 | ngrok 内网穿透（https://xxx.ngrok.io:443） | MacBook 端口映射到外网，5分钟配置 |
| 完全离线 | iPhone 本地 SQLite 缓存 | 离线可语音输入本地缓存，恢复联网后同步 |
| 规模扩大后 | 云端部署（方案2备选） | 迁移到 Railway/Render，无需 MacBook |

**方案2备选（规模扩大后）：**
将 Node.js 后端迁移到云端（ Railway / Render / Vercel ），API Key 放云端服务器，iPhone 永远能直调，SQLite 替换为 PostgreSQL + CloudKit。迁移成本：约半天，$5-10/月服务器费用。

### 为什么选 Node.js 而非 Python

技术选型理由：
- **LLM SDK**：火山方舟官方提供 Node.js SDK（ark SDK），Python SDK 为第三方封装，维护风险高
- **轻量**：Node.js + Express 足以支撑 MVP 阶段，无需 Django/Flask 的重量
- **iOS配合**：SwiftUI app 与 Node.js 后端通信，REST JSON 格式两边都简洁
- **实时性**：Node.js 异步 I/O 适合 LLM 调用这种 I/O 密集场景

Python 作为备选（如果未来换技术栈）：
- LangChain 生态成熟，适合复杂 Agent 编排
- arrow/pendulum 时间处理更强
- SQLAlchemy ORM 更完善
- dateutil.parser 日程解析现成可用

**当前决策：MVP 阶段选 Node.js，优先火山方舟官方 SDK 支持，未来如需复杂 Agent 逻辑可迁移 Python。**

### 单Agent架构（不过度设计）

```
用户语音
    ↓
┌─────────────────────────────┐
│   SFSpeechRecognizer        │  ← iOS本地，免费，语音→文字
│   录音时长上限：60秒          │  ← 超过1分钟自动截断
└─────────────────────────────┘
    ↓（文字）
┌─────────────────────────────┐
│   LLM（ark-code-latest）     │  ← iPhone直调火山方舟API
│                              │     HTTPS，带API Key
│  意图分解                     │
│  约束理解                     │
│  时间关系理解（"会后"="之前"）  │  ← 见增强prompt
│  歧义追问（不完整时直接问）      │
│  选项生成                     │
│  → 返回结构化JSON             │
└─────────────────────────────┘
    ↓（用户打钩确认）
┌─────────────────────────────┐
│      固定规则引擎（确定性）      │
│                              │
│  冲突检测：时间段重叠检查       │
│  日程写入本地SQLite（pending_schedules）│
│  → CloudKit云同步（可选）      │
│  → UNUserNotificationCenter  │
└─────────────────────────────┘
```

**为什么单Agent足够：**
- "10点开会、11点和王总通电话"这类意图解析，单次LLM调用即可完成
- 编排器Agent解决的是"跨域协作决策"问题，日程管理没有多域
- 加多一层框架只会增加维护成本，对产品体验零提升

## 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| iOS客户端 | SwiftUI + Xcode | 聊天界面、日程列表、月视图、语音输入 |
| iOS本地存储 | SQLite（GRDB.swift） | 本地日程、待确认选项、离线缓存 |
| iOS安全存储 | Keychain（SecItemAdd） | API Key加密存储 |
| 语音识别 | SFSpeechRecognizer | iOS原生，免费，本地运行，转文字后本地调LLM |
| LLM调用 | ark-code-latest | iPhone直调火山方舟API，HTTP HTTPS |
| 后端服务器 | Node.js + Express | 在MacBook上运行，仅SQLite，无LLM调用，无业务逻辑 |
| 通信协议 | HTTP REST | iPhone↔MacBook日程同步 |

## 功能规格

### 核心页面结构

```
App主界面（对话）
│
 ├── 右侧滑出 → 月视图（负一屏）
 │                ├── 完整月历
 │                ├── 点击日期 → 显示当天日程
 │                └── 点击日程条目 → 弹出编辑界面
 │
 └── 左侧滑出 → 日程列表
                  ├── 向上滚动 → 从当前时间往前推
                  ├── 日期筛选器
                  └── 点击日程 → 弹出编辑界面
```

### 对话界面交互流程

```
1. 用户长按语音按钮，说："10点开会议，11点和王总电话，提前15分钟通知"
2. SFSpeechRecognizer本地转文字
3. iPhone直调 ark-code-latest（带当前时间注入），LLM解析意图
   - LLM识别出两个独立日程，生成选项JSON
   - 若意图不完整，LLM返回追问内容，iPhone显示追问气泡
4. iPhone本地显示选项列表（每个带checkbox）：
   [
     {"id": "opt-1", "title": "10:00 开会", "reminderTime": 1744149900, "checked": false},
     {"id": "opt-2", "title": "11:00 和王总通话", "reminderTime": 1744153500, "checked": false}
   ]
5. 用户逐个打钩 → 打钩项变为"已确认"
6. 用户对某个选项点"修改" → 进入编辑界面
7. 所有选项确认后，点"生成日程"
8. iPhone写入本地SQLite，联网后自动同步到MacBook
```

### 意图不完整的处理

```
用户："10点开会，提前15分钟通知，开完会我要先整理一下"
AI追问："整理会议记录预计需要多长时间？"

用户："15分钟吧"
AI理解：整理记录=15分钟，通话应该在10:15之后
建议选项：
  - 10:00 开会
  - 10:15-10:30 整理记录
  - 10:30 和王总通话（提醒10:15）
```

### 歧义追问时机

- 时间范围不明确（"下午开会"→几点？）
- 持续时间缺失（"整理记录"→多久？）
- 约束条件模糊（"和王总通话前问一下"→问什么？）
- 直接追问，不猜，不假设

### 日程编辑界面（月视图和列表均可触发）

- 显示：标题、时间、提醒时间、地点、备注
- 可修改：标题、时间、提醒时间、地点、备注
- 删除：确认弹窗
- 保存：写入本地SQLite，联网后同步MacBook

### 日程完成状态

- 打钩框在每个日程条目左侧
- 完成打钩后：该条目变灰，文字中间加横线
- 不删除，保留历史记录

## 数据库设计

### 日程表（SQLite，在MacBook上）

```sql
CREATE TABLE schedules (
  id TEXT PRIMARY KEY,               -- UUID
  device_id TEXT NOT NULL,           -- iPhone设备标识，区分不同用户
  title TEXT NOT NULL,               -- 日程标题
  location TEXT,                      -- 地点（可选）
  notes TEXT,                         -- 备注（可选）
  start_time INTEGER NOT NULL,        -- Unix时间戳（秒），UTC
  end_time INTEGER,                   -- Unix时间戳（秒），null表示持续时间未知
  reminder_time INTEGER,               -- 提醒时间，Unix时间戳，UTC
  timezone TEXT DEFAULT 'Asia/Shanghai', -- 日程所属时区
  repeat_rule TEXT,                    -- 重复规则（可选），格式：FREQ=WEEKLY;BYDAY=MO（参考iCalendar）
  is_completed INTEGER DEFAULT 0,    -- 0=未完成，1=已完成
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE INDEX idx_schedules_device ON schedules(device_id);
CREATE INDEX idx_schedules_time ON schedules(start_time);
```

### 会话表（SQLite，在MacBook上）

```sql
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,               -- UUID，客户端生成
  device_id TEXT NOT NULL,          -- iPhone设备标识
  created_at INTEGER NOT NULL,       -- Unix时间戳（秒）
  last_active_at INTEGER NOT NULL    -- 最后活跃时间
);

-- session过期：last_active_at 超过72小时未更新则视为过期
-- 多个对话并行：每个对话对应独立session_id，并行无冲突

CREATE INDEX idx_sessions_device ON sessions(device_id);
CREATE INDEX idx_sessions_active ON sessions(last_active_at);
```

### iPhone本地表（SQLite，在iPhone上）

```sql
-- 待处理缓存：用户输入了语音但LLM未返回结果（离线时）
CREATE TABLE pending_input (
  id TEXT PRIMARY KEY,               -- UUID
  session_id TEXT NOT NULL,         -- 所属会话ID
  user_input TEXT NOT NULL,         -- 用户原始语音输入文字
  is_processed INTEGER DEFAULT 0,   -- 0=未处理（联网后需重跑LLM），1=已处理
  created_at INTEGER NOT NULL
);

-- 已确认待同步日程：用户离线时确认了日程，联网后直接POST到MacBook，不需要重跑LLM
CREATE TABLE pending_schedules (
  id TEXT PRIMARY KEY,               -- UUID（与MacBook的schedules表id一致）
  session_id TEXT NOT NULL,         -- 所属会话ID
  title TEXT NOT NULL,
  start_time INTEGER NOT NULL,
  end_time INTEGER,
  reminder_time INTEGER,
  location TEXT,
  notes TEXT,
  repeat_rule TEXT,
  is_completed INTEGER DEFAULT 0,
  is_synced INTEGER DEFAULT 0,       -- 0=未同步到MacBook，1=已同步
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- 同步策略：
-- 1. 联网检测到 pending_input is_processed=0 → 重新调LLM → 处理完成后标记 is_processed=1
-- 2. 联网检测到 pending_schedules is_synced=0 → POST /api/sync → 标记 is_synced=1
-- 3. 保留30天后清理
```

## API设计（iPhone客户端 ↔ MacBook服务器）

### 基础URL
- 局域网：`http://<MacBook局域网IP>:<端口>`
- 远程： ngrok 穿透后 URL（MVP阶段配置一次，固定域名）

> **注意**：MVP阶段不做HTTPS，局域网内直连。如使用ngrok穿透，外网通过HTTPS访问ngrok URL。

### 认证
所有请求带两个Header：
- `X-Device-ID`：iPhone设备标识，区分不同用户
- `Authorization`：Bearer Token认证，值为共享密钥

```http
GET /api/schedules
X-Device-ID: uuid-xxxx-yyyy-zzzz
Authorization: Bearer <SYNC_SECRET>
```

> **SYNC_SECRET**：MacBook服务器通过环境变量`SYNC_SECRET`配置，首次配置后告知iPhone用户，用户在App设置页输入并存入Keychain。

> **LLM调用在iPhone端**：对话解析不走MacBook服务器，iPhone直调火山方舟API。MacBook只做数据库存储。

### 时间格式
所有时间均为 **UTC Unix时间戳（秒）**，客户端负责本地时区转换。

### 接口

#### GET /api/ping
轻量连接检测，用于iPhone判断服务器是否可达。

```http
GET /api/ping
X-Device-ID: uuid-xxxx
Authorization: Bearer <SYNC_SECRET>
```

```json
// 响应
{
  "status": "ok",
  "timestamp": 1744227600
}
```

#### POST /api/sync
iPhone将本地已确认的日程批量同步到MacBook服务器

```json
// 请求（iPhone本地确认后，批量提交）
{
  "schedules": [
    {
      "id": "uuid-1",
      "title": "10:00 开会",
      "startTime": 1744150800,
      "endTime": null,
      "reminderTime": 1744149600,
      "location": null,
      "notes": null,
      "repeatRule": null,
      "isCompleted": false,
      "createdAt": 1744150000,
      "updatedAt": 1744150000
    },
    {
      "id": "uuid-2",
      "title": "11:00 和王总通话",
      "startTime": 1744154400,
      "endTime": null,
      "reminderTime": 1744153500,
      "location": null,
      "notes": null,
      "repeatRule": null,
      "isCompleted": false,
      "createdAt": 1744150000,
      "updatedAt": 1744150000
    }
  ]
}
// Headers: X-Device-ID: uuid-xxxx

// 响应
{
  "success": true,
  "syncedCount": 2
}
```

#### GET /api/schedules
获取日程列表（支持按日期过滤）

```http
GET /api/schedules?from=1744150800&to=1744237200
X-Device-ID: uuid-xxxx
```
- `from` 和 `to` 为UTC Unix时间戳，不传则返回全部（限制最近90天）

```json
// 响应
{
  "schedules": [
    {
      "id": "uuid-1",
      "title": "10:00 开会",
      "startTime": 1744150800,
      "endTime": null,
      "reminderTime": 1744149600,
      "isCompleted": false
    }
  ]
}
```

#### PUT /api/schedules/:id
修改日程

```http
PUT /api/schedules/uuid-1
X-Device-ID: uuid-xxxx
Content-Type: application/json
```

```json
// 请求
{
  "title": "10:00 开会（加参会人）",
  "startTime": 1744150800,
  "reminderTime": 1744149600
}

// 响应
{
  "success": true,
  "schedule": {
    "id": "uuid-1",
    "title": "10:00 开会（加参会人）",
    "startTime": 1744150800,
    "reminderTime": 1744149600,
    "isCompleted": false
  }
}
```

#### DELETE /api/schedules/:id
删除日程

```http
DELETE /api/schedules/uuid-1
X-Device-ID: uuid-xxxx
```

```json
// 响应
{
  "success": true
}
```

#### POST /api/schedules/:id/complete
标记完成

```http
POST /api/schedules/uuid-1/complete
X-Device-ID: uuid-xxxx
```

```json
// 响应
{
  "success": true,
  "isCompleted": true
}
```

### 冲突检测行为

当用户确认的日程与已有日程时间重叠时：
1. iPhone端LLM在生成选项时，`aiReasoning` 字段标注冲突
2. 用户仍可强制确认（系统允许重叠日程）
3. 月视图和列表中重叠日程正常展示（不强制调整）

> **注意**：`aiReasoning` 字段由iPhone端LLM生成，服务器仅透传存储，不做任何规则推理。

### 错误处理

| 错误场景 | 服务器行为 | 客户端行为 |
|---------|-----------|-----------|
| LLM返回格式异常 | 返回 500，记录原始响应供调试 | 显示"AI暂时无法解析，请重试" |
| 网络断开 | 超时返回 503 | 显示"无法连接服务器，请检查网络" |
| deviceId缺失 | 返回 401 | 引导重新打开App |
| 日程不存在 | 返回 404 | 显示"该日程已被删除" |

## LLM调用配置

### 超时与重试

iPhone直调ark-code-latest时使用以下配置：

| 参数 | 值 | 说明 |
|------|-----|------|
| 超时 | 30秒 | 请求超过30秒视为失败 |
| 重试次数 | 最多3次 | 指数退避：1s → 2s → 4s |
| 重试条件 | 网络错误、超时（HTTP 503/504） | 非接口错误（4xx）不重试 |

**超时后处理**：
1. 将用户输入存入本地 `pending_input` 表（is_processed=0）
2. 切换到文本输入模式，显示"语音输入已暂存，联网后自动处理"
3. 用户可手动切换到文本输入继续

### API Key首次配置

用户首次打开App时，通过设置页配置API Key：

1. App启动 → 检测Keychain无API Key → 跳转"设置API Key"页面
2. 用户粘贴火山方舟API Key（支持粘贴板）
3. App验证Key可用（发一个test请求）→ 成功则存入Keychain
4. 验证失败 → 提示"API Key无效，请检查后重试"

> **注意**：API Key约50字符，提供粘贴板按钮优化体验。

### API Key存储
- **存储方式**：iOS Keychain（`SecItemAdd` / `SecItemCopyMatching`）
- **首次配置**：通过App设置页粘贴API Key，验证后存入Keychain
- **安全等级**：Keychain数据加密存储，App删除后Keychain条目可保留（可选），需手动删除
- **永不硬编码**：API Key不出现在代码、UserDefaults、日志中

### iPhone本地数据存储
- **本地SQLite**：通过 `GRDB.swift` 或 `SQLite.swift` 管理
- **Keychain**：API Key
- **UserDefaults**：服务器URL配置、用户偏好设置

## 界面规格

### 设置页面（首次配置）

首次打开App时显示，配置完成后不再显示（除非用户主动进入）：

```
┌─────────────────────────────────┐
│  配置 API Key                    │
├─────────────────────────────────┤
│                                  │
│  输入你的火山方舟 API Key：        │
│  ┌───────────────────────────┐  │
│  │                          │  │
│  └───────────────────────────┘  │
│                                  │
│  ┌───────────────────────────┐  │
│  │ 📋 粘贴                   │  │
│  └───────────────────────────┘  │
│                                  │
│  ┌───────────────────────────┐  │
│  │        验证并保存          │  │
│  └───────────────────────────┘  │
│                                  │
│  服务器地址：                      │
│  ┌───────────────────────────┐  │
│  │ http://192.168.1.x:3000  │  │
│  └───────────────────────────┘  │
│  或：                            │
│  ┌───────────────────────────┐  │
│  │ https://300042.cn         │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

设置页包含：
- API Key输入框（支持粘贴板）
- 服务器地址（局域网IP或ngrok URL）
- SYNC_SECRET（共享密钥）
- 保存后验证连接

### 对话界面（主界面）
- 全屏聊天界面，底部语音按钮（圆形，Haptic反馈）
- 语音按钮左侧：文本输入框（可切换）
- AI回复气泡 + 选项列表（checkbox形式）
- 每个选项：checkbox + 标题 + 提醒时间 + "修改"按钮

### 选项编辑Sheet（点击"修改"触发）

弹出底部Sheet，顶部标题显示"修改日程"，包含：

```
┌─────────────────────────────────┐
│  × 取消          修改日程     ✓ 保存 │
├─────────────────────────────────┤
│  标题                                 │
│  ┌─────────────────────────────┐ │
│  │ 10:00 开会                  │ │
│  └─────────────────────────────┘ │
│                                 │
│  开始时间        结束时间        │
│  ┌──────────┐  ┌──────────┐   │
│  │ 10:00   │  │ 11:00   │   │
│  └──────────┘  └──────────┘   │
│                                 │
│  提醒时间                      │
│  ┌─────────────────────────────┐ │
│  │ 提前15分钟              ▼  │ │
│  └─────────────────────────────┘ │
│                                 │
│  或用语音重新描述这个日程：      │
│  ┌─────────────────────────────┐ │
│  │ 🎤 长按说话...              │ │
│  └─────────────────────────────┘ │
└─────────────────────────────────┘
```

**修改 Sheet 交互说明：**

| 字段 | 操作方式 | 说明 |
|------|---------|------|
| 标题 | 手动键盘输入 | 可直接编辑文字 |
| 开始/结束时间 | 时间选择器 | UIDatePicker |
| 提醒时间 | 下拉选择 | 选项：准时、提前5分钟、提前15分钟、提前30分钟、自定义 |
| 语音重新描述 | 长按语音按钮 | 语音输入 → LLM重新解析 → 自动填入各字段 |

- 点击"保存"：更新内存中该选项的字段，返回对话界面，选项列表刷新显示
- 点击"取消"或×：丢弃修改，返回对话界面
- 语音重新描述后，LLM结果自动填入各字段，用户可再手动微调

### 月视图（负一屏）
- iOS系统日历风格月视图
- 有日程的日期数字加粗或加圆点
- 点击日期展开当天日程列表
- 点击单个日程 → 弹出编辑Sheet
- **end_time为null的日程**：显示为该时间点的时刻事件（如"10:00 开会"），月视图上该日期的时间格内以单行展示

### 日程列表（左侧滑）
- 垂直滚动列表，每项：checkbox + 标题 + 时间 + 提醒时间
- 滑动到顶部显示当前日期快捷入口
- 日期切换器（横向日期标签）

### 配色与主题
- 明亮模式优先（支持Dark Mode）
- 设计令牌系统：颜色、字体、间距独立定义
- MVP阶段使用系统默认蓝色为主色调

## 开发阶段

### 阶段一：核心闭环（4-6周）

**1a. MacBook后端核心（Node.js + SQLite）**
- 项目初始化：Express服务器
- /api/schedules CRUD接口（存储日程）
- /api/sessions 管理接口（创建、更新、过期清理）
- /api/sync 接口（接收离线缓存同步）
- SQLite数据库初始化（schedules + sessions）
- MacBook本地运行测试

**1b. iOS对话界面（SwiftUI）**
- 项目初始化：Xcode + SwiftUI
- SFSpeechRecognizer语音输入（60秒上限截断）
- 对话气泡UI（用户气泡 + AI气泡）
- 选项列表UI（checkbox + 标题 + 提醒 + 修改按钮）
- LLM调用层（ark-code-latest HTTP直调，带API Key）
- Haptic Engine反馈（长按开始/结束录音）

**1c. iOS日程列表 + 月视图**
- 日程列表（左滑）
- 月视图（右滑负一屏）
- 日程编辑Sheet
- 本地SQLite存储

**1d. 离线缓存 + 同步**
- 离线缓存表设计（SQLite on iPhone）
- 联网时自动同步未提交数据到MacBook
- 同步状态UI（同步成功/失败提示）

**1e. 联调与测试**
- 三屏交互流程跑通
- 月视图点击编辑跑通
- 日程完成状态跑通
- 局域网直连MacBook测试

### 阶段二：体验打磨（2-3周）

- **网络切换**：局域网直连 / ngrok远程 / 离线缓存三种模式
- **通知系统**：UNUserNotificationCenter集成（本地通知，App被杀掉也能推）
- **语音识别失败策略**：
  - 语音识别返回空或失败 → 自动切换到文本输入模式，提示"请手动输入"
  - 连续3次语音识别失败 → 弹窗建议检查网络或麦克风权限
  - 权限被拒绝 → 直接展示文本输入框
- Dark Mode支持
- 重复日程支持
- ngrok内网穿透配置（外出时远程访问MacBook）

### 阶段三：后续（看情况）
- CloudKit云同步（用户iCloud账号，数据多端同步）
- 团队协作（互看空闲时间，好友/组织添加）
- 方案2云端部署（规模扩大后迁移到Railway/Render）
- 安卓/Web客户端

## LLM Prompt设计（核心）

### 系统提示词

```
你是一个日程管理助手。用户通过自然语言描述他们的日程安排。
你的任务是解析用户的意图，生成明确的日程选项供用户确认。

【每次请求时，以下内容由客户端动态注入，禁止硬编码】
当前时间：{CURRENT_TIME} （北京时间 UTC+8）
今天是：{CURRENT_DATE} （格式：YYYY-MM-DD）

所有时间必须使用UTC Unix时间戳（秒），不要使用ISO 8601字符串。

时区规则（重要）：
- 用户说"10点开会"若未指定时区，理解为 Asia/Shanghai (UTC+8)
- 计算提醒时间时：先将用户时间转换为UTC，再减去提前分钟数
- 例如：用户说"10点提前15分钟通知"，当前时间为 2026-04-10 09:00 CST
  → startTime = 1744227600 (2026-04-10 10:00 CST = 02:00 UTC)
  → reminderTime = startTime - 15*60 = 1744226700 (2026-04-10 09:45 CST)

时间依赖关系理解（重要）：
- "会后" = 前一个日程的 end_time（如果前一个日程有结束时间）
- "之前" = 某时间点之前，如"下班前"默认为17:00
- "提前X分钟" = start_time - X*60
- "紧接着" = 无间隙，后一个日程 start_time = 前一个日程的 end_time
- "中间休息10分钟" = 日程之间留10分钟空白
- "明天" = 当前日期+1天
- "后天" = 当前日期+2天
- "下周一" = 下一个周一（若今天已是周一则为今天起算的7天后）

示例（当前时间：2026-04-10 09:00 CST）：
输入: "10点开会1小时，会后和客户电话半小时"
输出:
{
  "status": "options_ready",
  "options": [
    {
      "title": "10:00 开会",
      "startTime": 1744227600,
      "endTime": 1744231200,
      "reminderTime": 1744226700,
      "aiReasoning": "识别到开会1小时，10:00-11:00，添加提前15分钟提醒"
    },
    {
      "title": "和客户电话",
      "startTime": 1744231200,
      "endTime": 1744233000,
      "reminderTime": 1744232100,
      "aiReasoning": "会后立即通话30分钟，11:00-11:30，添加提前15分钟提醒"
    }
  ]
}

输入: "明天下午3点约刘总"
输出:
{
  "status": "options_ready",
  "options": [
    {
      "title": "约刘总",
      "startTime": 1744357200,
      "endTime": null,
      "reminderTime": 1744356300,
      "aiReasoning": "明天（2026-04-11）下午3点 = 15:00 CST，添加提前15分钟提醒"
    }
  ]
}

规则：
1. 意图不完整时，必须追问，不猜
2. 每个日程必须包含：title、startTime（Unix秒戳）、reminderTime（Unix秒戳）
3. 有明确持续时间的日程必须包含endTime
4. "提前X分钟通知"必须解析为 startTime 减 X * 60 秒
5. 多个日程用 options 数组返回
6. 意图完整时返回 status: "options_ready"
7. 意图不完整时返回 status: "needs_info"，追问内容放入 followUp.question 字段

输出格式（JSON，key为英文，内容为中文）：
{
  "status": "options_ready",
  "options": [
    {
      "title": "日程标题",
      "startTime": 1744227600,
      "endTime": null,
      "reminderTime": 1744226700,
      "aiReasoning": "你对这条日程的理解说明"
    }
  ]
}

意图不完整时：
{
  "status": "needs_info",
  "followUp": {
    "question": "追问问题",
    "context": "追问的背景说明"
  }
}
```

### LLM调用模式：新建 vs 修改

LLM有两种工作模式，iPhone端根据用户操作切换。

#### 模式一：新建日程（Creation Mode）

用户直接说一段新日程，发送内容：
```json
{
  "mode": "create",
  "currentTime": "2026-04-10 14:32 CST",
  "currentDate": "2026-04-10",
  "userInput": "10点开会议，11点和王总电话，提前15分钟通知"
}
```

#### 模式二：修改日程（Modification Mode）

用户在选项列表点"修改"，用语音重新描述该日程。**必须携带原始日程上下文**，否则LLM无法理解"改成下午3点"是在改哪个日程。

**发送内容：**
```json
{
  "mode": "modify",
  "currentTime": "2026-04-10 14:32 CST",
  "currentDate": "2026-04-10",
  "originalOption": {
    "title": "10:00 开会",
    "startTime": 1744227600,
    "endTime": null,
    "reminderTime": 1744226700
  },
  "userInput": "把时间改到下午3点"
}
```

**修改模式的LLM Prompt补充规则：**

```
【修改模式规则】
用户要求修改一个已有日程。请根据用户的修改意图，生成修改后的日程选项。

收到以下原始日程信息：
- 原标题：{original_title}
- 原开始时间：{original_startTime}（UTC Unix秒戳）
- 原提醒时间：{original_reminderTime}（UTC Unix秒戳）

语义理解规则：
- "改成X点" / "改到X点" → 只修改startTime，不改变其他字段
- "提前/推迟X小时" → startTime加减X*3600秒
- "延长到X点" → 修改endTime
- "提醒改成提前Y分钟" → 只修改reminderTime
- "把XXX的时间改到..." → 先识别XXX是指哪个已有日程，再用该日程的原始数据替换

歧义处理（重要）：
- 如果用户说"改成下午3点"但当前有多个日程都是下午 → 返回 status: "needs_info"，追问"您要修改哪个日程？"
- 如果用户说的日程在历史记录中找不到 → 返回 status: "needs_info"，追问"您要修改的是哪个日程？"

修改后输出格式（与新建相同，但aiReasoning说明做了哪些修改）：
{
  "status": "options_ready",
  "options": [
    {
      "title": "15:00 开会",
      "startTime": 1744261200,
      "endTime": null,
      "reminderTime": 1744260300,
      "aiReasoning": "将原日程10:00开会改到15:00，提醒时间相应调整为14:45"
    }
  ]
}

示例：
原始日程：{"title": "10:00 开会", "startTime": 1744227600, "reminderTime": 1744226700}
用户输入："改成下午3点"
输出：
{
  "status": "options_ready",
  "options": [
    {
      "title": "15:00 开会",
      "startTime": 1744261200,
      "reminderTime": 1744260300,
      "aiReasoning": "将开始时间从10:00改到15:00，提醒从09:45改到14:45"
    }
  ]
}
```

## 部署与运行

### MacBook服务器

```bash
# 在MacBook上
node server/index.js

# 服务器监听 0.0.0.0:3000
# iPhone客户端通过局域网IP访问，如 http://192.168.1.x:3000
```

### ngrok内网穿透配置（外出时远程访问）

```bash
# 1. 注册 ngrok (https://ngrok.com)，获取 authtoken
# 2. 安装 ngrok
brew install ngrok

# 3. 配置 authtoken
ngrok config add-authtoken <YOUR_TOKEN>

# 4. 配置固定域名（你的域名：300042.cn）
#    在 ngrok 控制台或配置文件中设置：
#    ngrok config add domain 300042.cn
#    或者在 ngrok.yml 中添加：
#    tunnels:
#      http:
#        addr: 3000
#        domain: 300042.cn

# 5. 启动隧道（MacBook上运行）
ngrok http 3000 --domain=300042.cn

# 6. 输出示例：
# Forwarding  https://300042.cn -> http://localhost:3000
# iOS客户端配置此固定URL即可远程访问
```

> **注意**：ngrok免费版不支持固定域名，需要ngrok付费版（约$5/月）才能绑定自定义域名300042.cn。

### iOS客户端

```bash
# Xcode打开 ios/ScheduleApp.xcodeproj
# 连接真机（模拟器不支持SFSpeechRecognizer）
# 运行即用
```

## 成功标准

1. 用户在对话界面说一段包含多个日程的话，AI正确解析并列出选项
2. 用户打钩确认后，日程出现在月视图和列表中
3. 点击月视图中的日程可直接编辑/删除
4. 日程列表中完成打钩后，日程变灰+删除线
5. 意图不完整时，AI正确追问而不是乱猜
6. 时间依赖关系理解正确（"会后"、"提前X分钟"等）
7. 语音输入超过60秒自动截断
8. 语音识别失败时自动降级到文本输入
9. 离线时输入缓存到本地，联网后自动同步
10. 10-50人小范围测试，MacBook服务器稳定运行

## 开放问题

1. **数据备份**：数据全在MacBook本地，MacBook坏了数据就没了。MVP阶段接受这个风险，后续可加手动导出（JSON文件）或 CloudKit 同步。

2. **多设备支持**：同一用户有两台iPhone？目前不支持，deviceId按设备算。后续可加用户体系（手机号注册）支持跨设备同步。

3. **方案2云端部署（规模扩大后）**：当团队扩大到50人以上，或需要跨城市/跨网络访问时，将Node.js后端迁移到 Railway/Render 云端。API Key 放云端服务器，SQLite 替换为 PostgreSQL + CloudKit，iPhone 永远直调云端API，无需 MacBook 在线。迁移成本约半天，服务器费用 $5-10/月。

## 通知系统（UNUserNotificationCenter）

### 通知触发时机
- 日程提醒时间到达时，通过 `UNUserNotificationCenter` 发送本地推送
- App 被杀掉也能推送（iOS系统接管）
- 不依赖 MacBook 服务器

### 实现方案
```
iOS端:
UNUserNotificationCenter
  → requestAuthorization(options: [.alert, .sound, .badge])
  → add(request: UNNotificationRequest) // 创建日程时同时注册
  → 到达提醒时间 → 系统推送通知
```

### 通知权限申请
- 第一次创建日程时申请（不提前申请，避免用户反感）
- 申请时机：用户确认第一个日程选项后

## App生命周期处理

### 前台/后台切换

| 场景 | 处理 |
|------|------|
| App切到后台 | 停止当前录音（未完成语音存入pending_input）；取消未完成的LLM请求；保留已确认日程在本地SQLite |
| App回到前台 | 检测pending_input未处理记录（is_processed=0）→ 重新调LLM；检测pending_schedules未同步记录（is_synced=0）→ POST /api/sync；检测服务器连接（GET /api/ping） |
| App被杀掉 | UNUserNotificationCenter通知不受影响（iOS系统接管）；本地数据保留，下次启动时同步 |

### 网络切换检测

| 场景 | 处理 |
|------|------|
| 局域网直连可用 | 直连MacBook（最低延迟） |
| 仅ngrok远程可用 | 切换到ngrok URL |
| 完全离线 | 所有操作缓存本地pending_*表；显示离线图标；联网后自动同步 |

| 失败场景 | 处理策略 |
|---------|---------|
| 麦克风权限被拒绝 | 直接展示文本输入框，不弹语音按钮 |
| 语音识别返回空文字 | 自动切换到文本输入模式，提示"请手动输入" |
| 连续3次识别失败 | 弹窗提示"语音识别不稳定，请检查网络或麦克风" |
| 网络断开（离线） | 缓存语音输入到本地SQLite，联网后重试识别 |
| 录音超时（60秒） | 强制截断，按现有内容处理 |

## 网络中断本地缓存策略

### 缓存时机
- 用户完成语音输入 → 立即写入本地 `pending_input` 表（LLM未处理，离线时）
- 用户离线确认日程后 → 写入本地 `pending_schedules` 表（is_synced=0）
- LLM调用失败（网络断） → 写入 `pending_input` 表

### 同步时机
- 联网检测到 `pending_input` 未处理记录（is_processed=0）→ 重新调LLM → 完成后标记 is_processed=1
- 联网检测到 `pending_schedules` 未同步记录（is_synced=0）→ POST /api/sync → 标记 is_synced=1
- 用户主动进入App → 检测网络，触发同步

### 同步冲突处理
- 以服务器时间为准（`updated_at` 更大者保留）
- 本地数据优先展示，同步在后台静默进行

### 同步状态UI
- App 内显示同步状态图标（同步中/同步成功/离线）
- 失败时显示红色图标，用户可点击重试

---

## 附录：与原文档的差异说明

| 原文档内容 | 调整后 | 原因 |
|-----------|--------|------|
| 多Agent架构（编排器+日程管理） | 单Agent + 固定规则引擎 | MVP阶段多Agent无收益，增加维护复杂度 |
| Vercel SDK作为服务端框架 | Node.js + Express | Vercel不适合跑LLM长连接 |
| iOS + 安卓 + Web三端 | 仅iOS MVP | 没有技术背景，三端同步管理成本过高 |
| 薄客户端架构 | 本地SQLite + MacBook集中推理 | 初期无需云端同步，简化架构 |
| "10-50人测试不考虑验证" | 明确为同事小范围使用 | 实操可行性更清晰 |

---

## Assignment（下一步行动）

1. 在MacBook上初始化Node.js项目，安装Express、SQLite（better-sqlite3）
2. 搭建SQLite数据库表结构（schedules + sessions，仅两个表）
3. 实现 `/api/schedules` CRUD接口和 `/api/sync` 同步接口
4. 实现 `/api/sessions` 管理接口（创建、更新、过期清理）
5. 在Xcode创建iOS项目原型，集成SFSpeechRecognizer（60秒截断）
6. iOS端接入 ark-code-latest API（HTTP直调火山方舟，API Key存本地）
7. 实现本地SQLite缓存层（pending_input + pending_schedules表）
8. 最小闭环测试：语音输入 → LLM解析 → 选项展示 → 日程写入本地SQLite
9. 配置ngrok穿透，测试远程访问MacBook
